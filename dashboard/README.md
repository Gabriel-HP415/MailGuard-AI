# MailGuard-AI Dashboard

Bootstrap 5 + Chart.js frontend served as static files.

Run locally:

```bash
python -m http.server 5500
# open http://localhost:5500
```

By default the dashboard expects the backend at `http://localhost:8000`.
You can change this in `js/config.js`.